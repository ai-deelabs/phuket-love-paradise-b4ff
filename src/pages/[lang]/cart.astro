---
import CartView from '../../views/CartView.astro';
import type { Lang } from '../../i18n/config';

export function getStaticPaths() {
  return [{ params: { lang: 'en' } }, { params: { lang: 'ru' } }];
}
const lang = Astro.params.lang as Lang;
---

<CartView lang={lang} />
